<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.scene.text.Text?>

<BorderPane xmlns="http://javafx.com/javafx/21" xmlns:fx="http://javafx.com/fxml/1"
            fx:controller="controllers.ViewApplicationsController"
            prefHeight="800.0" prefWidth="1200.0"
            style="-fx-background-color: #F5F5F5;">

    <!-- Header -->
    <top>
        <VBox styleClass="header" style="-fx-background-color: #1976D2;">
            <HBox alignment="CENTER_LEFT" spacing="20" style="-fx-padding: 15 30;">
                <Button text="← Retour" styleClass="btn-secondary" onAction="#handleBack"
                        style="-fx-background-color: #757575; -fx-text-fill: white; -fx-padding: 8 20; -fx-border-radius: 8; -fx-background-radius: 8;"/>
                <Text text="AGRIFLOW" style="-fx-font-size: 24px; -fx-font-weight: bold; -fx-fill: white;"/>
                <Region HBox.hgrow="ALWAYS"/>
                <Text fx:id="headerTitle" text="Candidatures reçues" style="-fx-font-size: 18px; -fx-fill: white;"/>
            </HBox>
        </VBox>
    </top>

    <center>
        <VBox spacing="25" style="-fx-padding: 40;">

            <!-- Titre et informations -->
            <VBox spacing="10">
                <HBox alignment="CENTER_LEFT" spacing="15">
                    <Text text="📋 Candidatures reçues" style="-fx-font-size: 28px; -fx-font-weight: bold;"/>
                </HBox>
                <HBox spacing="15">
                    <VBox spacing="5">
                        <Text text="Demande :" style="-fx-font-size: 13px; -fx-fill: #757575;"/>
                        <Text fx:id="requestTitleText" text="Chargement..."
                              style="-fx-font-size: 16px; -fx-font-weight: bold; -fx-fill: #2E7D32;"/>
                    </VBox>
                    <Region HBox.hgrow="ALWAYS"/>
                    <VBox spacing="5" alignment="CENTER_RIGHT">
                        <Text fx:id="applicationsCountText" text="0 candidatures"
                              style="-fx-font-size: 18px; -fx-font-weight: bold; -fx-fill: #1976D2;"/>
                    </VBox>
                </HBox>
            </VBox>

            <!-- Tableau des candidatures -->
            <VBox styleClass="card" style="-fx-background-color: white; -fx-padding: 25; -fx-background-radius: 12; -fx-effect: dropshadow(gaussian, rgba(0,0,0,0.1), 10, 0, 0, 2);">

                <TableView fx:id="applicationsTable" prefHeight="450">
                    <columns>
                        <TableColumn fx:id="fullNameColumn" text="Nom complet" prefWidth="180"/>
                        <TableColumn fx:id="phoneColumn" text="Téléphone" prefWidth="120"/>
                        <TableColumn fx:id="emailColumn" text="Email" prefWidth="200"/>
                        <TableColumn fx:id="experienceColumn" text="Expérience" prefWidth="100"/>
                        <TableColumn fx:id="expectedSalaryColumn" text="Salaire (DT)" prefWidth="110"/>
                        <TableColumn fx:id="motivationColumn" text="Motivation" prefWidth="250"/>
                        <TableColumn fx:id="statusColumn" text="Statut" prefWidth="120"/>
                    </columns>
                    <columnResizePolicy>
                        <TableView fx:constant="CONSTRAINED_RESIZE_POLICY"/>
                    </columnResizePolicy>
                </TableView>

                <!-- Message si aucune candidature -->
                <VBox fx:id="emptyStateBox" alignment="CENTER" spacing="15"
                      style="-fx-padding: 60;" visible="false" managed="false">
                    <Text text="📭" style="-fx-font-size: 48px;"/>
                    <Text text="Aucune candidature reçue"
                          style="-fx-font-size: 18px; -fx-fill: #757575; -fx-font-weight: bold;"/>
                    <Text text="Les candidatures apparaîtront ici une fois que des agriculteurs auront postulé à votre demande."
                          style="-fx-font-size: 14px; -fx-fill: #BDBDBD; -fx-text-alignment: center; -fx-wrap-text: true;"
                          wrappingWidth="400"/>
                </VBox>

            </VBox>

            <!-- Boutons d'action -->
            <HBox spacing="15" alignment="CENTER_LEFT">
                <Button text="✅ Accepter" styleClass="btn-success" onAction="#handleApprove"
                        style="-fx-background-color: #2E7D32; -fx-text-fill: white; -fx-padding: 12 30; -fx-font-size: 15px; -fx-font-weight: bold; -fx-border-radius: 8; -fx-background-radius: 8;"/>
                <Button text="❌ Rejeter" styleClass="btn-danger" onAction="#handleReject"
                        style="-fx-background-color: #D32F2F; -fx-text-fill: white; -fx-padding: 12 30; -fx-font-size: 15px; -fx-font-weight: bold; -fx-border-radius: 8; -fx-background-radius: 8;"/>
                <Region HBox.hgrow="ALWAYS"/>
                <Button text="🔄 Rafraîchir" styleClass="btn-secondary" onAction="#handleRefresh"
                        style="-fx-background-color: #757575; -fx-text-fill: white; -fx-padding: 12 30; -fx-font-size: 15px; -fx-border-radius: 8; -fx-background-radius: 8;"/>
            </HBox>

            <!-- Légende des statuts -->
            <VBox spacing="10" style="-fx-background-color: #E3F2FD; -fx-padding: 20; -fx-border-radius: 8; -fx-background-radius: 8;">
                <Text text="ℹ️ Statut des candidatures" style="-fx-font-size: 14px; -fx-font-weight: bold;"/>
                <HBox spacing="30">
                    <HBox spacing="8" alignment="CENTER_LEFT">
                        <Text text="●" style="-fx-fill: #FFA726; -fx-font-size: 20px;"/>
                        <Text text="EN ATTENTE : En cours d'examen" style="-fx-font-size: 13px;"/>
                    </HBox>
                    <HBox spacing="8" alignment="CENTER_LEFT">
                        <Text text="●" style="-fx-fill: #66BB6A; -fx-font-size: 20px;"/>
                        <Text text="ACCEPTÉE : Candidat sélectionné" style="-fx-font-size: 13px;"/>
                    </HBox>
                    <HBox spacing="8" alignment="CENTER_LEFT">
                        <Text text="●" style="-fx-fill: #EF5350; -fx-font-size: 20px;"/>
                        <Text text="REJETÉE : Candidature refusée" style="-fx-font-size: 13px;"/>
                    </HBox>
                </HBox>
            </VBox>

        </VBox>
    </center>
</BorderPane>